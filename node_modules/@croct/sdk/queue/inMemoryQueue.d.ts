import { Queue } from './queue';
export declare class InMemoryQueue<T> implements Queue<T> {
    private queue;
    constructor(...values: T[]);
    all(): T[];
    getCapacity(): number;
    isEmpty(): boolean;
    push(value: T): void;
    peek(): T | null;
    shift(): T;
    length(): number;
}
