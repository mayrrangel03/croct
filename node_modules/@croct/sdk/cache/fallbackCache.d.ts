import { Cache } from './cache';
export declare class FallbackCache implements Cache {
    private readonly caches;
    constructor(...caches: Cache[]);
    get(): string | null;
    put(value: string): void;
    clear(): void;
}
